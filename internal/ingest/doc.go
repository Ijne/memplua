// Package ingest defines chunks, analysis batches, conspects, durable jobs, and
// their ports. Worker implements leased execution with cancellation, bounded
// retry, backoff, and explicit backpressure semantics.
package ingest
