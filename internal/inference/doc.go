// Package inference integrates local AI runtimes. LlamaClient sends extraction
// requests to a llama.cpp-compatible HTTP server; Supervisor optionally owns the
// server process; Windows-native adapters wrap Whisper and Silero.
package inference
