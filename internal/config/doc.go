// Package config loads, validates, resolves, and persists typed application
// configuration. Effective values are composed in the order defaults, TOML,
// KNOWLEDGECRAWLER_* environment variables, and command-line overrides.
package config
