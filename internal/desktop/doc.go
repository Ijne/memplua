// Package desktop hosts the Windows Wails shell. It owns native windows, tray,
// autostart, file pickers, window-state persistence, single-instance activation,
// and the short-lived credential that connects embedded UI code to the local API.
package desktop
